{
  "name": "@donezone/template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "contracts/*",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "build": "bun run --cwd frontend build",
    "local": "bunx done dev & bun run --cwd frontend dev",
    "preview": "bun run--cwd frontend preview",
    "contracts:build": "bunx done build",
    "contracts:deploy": "bunx done deploy",
    "contracts:dev": "bunx done dev"
  },
  "devDependencies": {
    "@donezone/cli": "^0.1.0",
    "@donezone/contract-types": "^0.1.0",
    "@types/bun": "^1.1.10",
    "typescript": "^5.8.3"
  }
}